{
    "version": 2,
    "builds": [{
        "src": "app.py",
        "use": "@vercel/python"
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "app.py"
    }],
    "scripts": {
        "prestart": "export PATH=$PWD/bin:$PATH"
    }
}